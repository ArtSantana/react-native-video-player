import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  fullScreen: {
    backgroundColor: '#000',
    height: '100%',
    position: 'absolute',
    width: '100%',
  },
});

export default styles;
